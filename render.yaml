services:
  - type: web
    name: credit-risk-api
    runtime: python
    pythonVersion: 3.12
    buildCommand: pip install -r requirements.txt && python -m src.train
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
